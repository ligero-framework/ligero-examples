application {
    mainClass = 'com.example.redis.Application'
}

dependencies {
    implementation 'com.ligero:ligero-redis:0.2.0-SNAPSHOT'   // brings ligero-auth transitively
    implementation 'redis.clients:jedis:5.2.0'                 // to build the JedisPool
}

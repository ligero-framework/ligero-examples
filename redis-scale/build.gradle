application {
    mainClass = 'com.example.redis.Application'
}

dependencies {
    implementation 'com.ligeroframework:ligero-redis:0.5.0'   // brings ligero-auth transitively
    implementation 'redis.clients:jedis:5.2.0'                 // to build the JedisPool
}
